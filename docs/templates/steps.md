
> ❗️Во всех шаблонах:  
> • Контекст = ListYB R1 — Android, локально, Drift  
> • Использовать финальное ТЗ из `docs/r1-spec.md` в репозитории.  
> • Соблюдать структуру папок из `docs/architecture.md`.  
> • Учитывать UX Undo (3 сек) для удаления/архивирования, как в ТЗ.

---

## 🔷 R1‑01 — Drift DB (v1)

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Начинаем реализацию R1‑01: Drift DB (v1).

Нужно:
- Добавить зависимости в pubspec.yaml (drift, drift_dev, sqlite3_flutter_libs, path_provider, build_runner).
- Создать AppDatabase с таблицами lists и items (схема из docs/db/schema.sql).
- Создать DAO:
  - ListsDao: CRUD, watchAll (вкл. параметр includeArchived).
  - ItemsDao: CRUD, watchByList(listId, query?, completed?), reorder (транзакция).
- Настроить миграции (MigrationStrategy) — schema v1.
- Подготовить dev-seed (опционально, флаг dev).
- Примеры unit‑тестов для DAO (CRUD + watch + reorder транзакционно).

Дай:
- Готовый блок pubspec.yaml.
- Код AppDatabase и моделей (Drift аннотации).
- Код ListsDao и ItemsDao.
- Пример тестов (test/...).
- Инструкции по запуску генерации: `dart run build_runner build -d`.

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

## 🔷 R1‑02 — Репозитории и Use‑cases

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Начинаем реализацию R1‑02: Репозитории и Use‑cases.

Нужно:
- Определить domain/entities: YbList, YbItem.
- Определить abstract repositories: ListsRepository, ItemsRepository.
- Реализовать имплементации в data/ через DAO Drift.
- Добавить мапперы DB↔Entity.
- Добавить use‑cases: create/rename/archive/delete list; add/update/toggle/reorder/delete item; watch‑потоки; counts().
- Покрыть базовыми unit‑тестами (валидаторы, happy‑path, edge cases).

Дай:
- Структуру папок (domain/data), интерфейсы и реализации.
- Мапперы.
- Примеры тестов use‑cases.

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

## 🔷 R1‑03 — Навигация и диплинки (go_router)

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Начинаем реализацию R1‑03: Навигация и диплинки.

Нужно:
- Подключить go_router и MaterialApp.router.
- Маршруты: "/", "/list/:id", "/settings" ("/search" опционально).
- Настроить диплинки Android (AndroidManifest, scheme listyb://).
- Обработку входящих URI (холодный/горячий старт), корректный back stack.
- Примеры: listyb://home, listyb://list/<id>.

Дай:
- Код router.dart с конфигурацией маршрутов.
- Изменение AndroidManifest (intent‑filter).
- Пример кода обработки диплинков.
- Команды ADB для проверки.

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

## 🔷 R1‑04 — UI Home (списки) + Undo для delete/archive

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Начинаем реализацию R1‑04: UI Home.

Нужно:
- Экран Home: AppBar (название + Settings), FAB «Создать список».
- Список карточек: название + бейдж (open/total).
- Действия по долгому нажатию: Переименовать / Архивировать(или Разархивировать) / Удалить.
- Пустое состояние (Empty state).
- Snackbar с Undo (3 сек) для удаления и архивирования:
  - Тексты i18n: snackbar.list_deleted, snackbar.list_archived, snackbar.undo.
  - Логика восстановления (транзакция) при нажатии «Отменить».
- Интеграция с use‑cases и провайдерами Riverpod.

Дай:
- Код страницы, виджетов карточек, меню действий.
- Snackbar/Undo обвязку (сервис/хелпер).
- Тесты виджетов на основные сценарии.

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

## 🔷 R1‑05 — UI ListDetails (элементы) + поиск/фильтры/Drag&Drop + Undo

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Начинаем реализацию R1‑05: UI ListDetails.

Нужно:
- AppBar с названием списка и меню списка.
- Поле «Быстро добавить…» (Enter добавляет).
- Фильтры: Все / Открытые / Выполненные.
- Поиск по подстроке (регистронезависимо).
- Список элементов с чекбоксами, drag handle, long‑press меню (Редактировать/Удалить).
- Drag&Drop с сохранением позиции (ItemsRepository.reorder транзакционно).
- Пустое состояние.
- Snackbar с Undo (3 сек) для удаления и архивирования элемента:
  - i18n: snackbar.item_deleted, snackbar.item_archived, snackbar.undo.
  - Восстановление элемента и позиции при Undo.
- Интеграция с провайдерами Riverpod.

Дай:
- Код страницы и ключевых виджетов.
- Реализацию DnD + сохранения order.
- Snackbar/Undo для item.
- Тесты виджетов (добавление, поиск, фильтры, чекбокс, DnD, Undo).

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

## 🔷 R1‑06 — Settings + i18n (RU/EN) + темы

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Начинаем реализацию R1‑06: Settings + i18n + темы.

Нужно:
- Подключить flutter_localizations, intl; сгенерировать arb (app_en.arb, app_ru.arb).
- Настроить локализацию во всём приложении (без хардкода).
- Экран Settings: переключатели темы (System/Light/Dark) и языка (RU/EN).
- Хранение настроек (SharedPreferences или простой settings‑репозиторий).
- Переключение без перезапуска.
- Раздел «О приложении»: MIT License.

Дай:
- Изменения в app.dart (localizationsDelegates, supportedLocales).
- Код SettingsPage и хранения настроек.
- Обновлённые arb с ключами, включая snackbar.undo и сообщения из ТЗ.

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

## 🔷 R1‑07 — Тесты и статический анализ

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Начинаем реализацию R1‑07: Тесты и линтер.

Нужно:
- Unit‑тесты: репозитории и use‑cases (CRUD, поиск/фильтры, reorder, Undo‑flows).
- Widget‑тесты: Home и ListDetails (основные сценарии).
- Настроить и применить линтер (flutter_lints или very_good_analysis).
- Убедиться, что `flutter analyze` чистый.

Дай:
- Набор тестов с примерами.
- Конфигурацию линтера и инструкции запуска.

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

## 🔷 R1‑08 — CI Android (GitHub Actions)

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Начинаем реализацию R1‑08: CI Android.

Нужно:
- GitHub Actions workflow для Android:
  - setup Flutter stable
  - flutter pub get
  - flutter analyze
  - flutter test
  - flutter build apk --release
  - upload artifact
- Версионирование: 0.1.0+1.

Дай:
- Файл .github/workflows/android.yml (готовый).
- Короткие инструкции по статусу в PR и скачиванию артефактов.

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

## 🔷 (Опционально) Подготовка Issues и веток

```
[Context: ListYB R1 — Android, локально, Drift]
Репозиторий: https://github.com/yuriy-bak/ListYB
Используй финальное ТЗ из docs/r1-spec.md.

Во вложении zip-архив с файлами с моим кодом. Я изменил его расширение на tsv. Тебе надо изменить снова расширение на zip, распаковать и взять мои данные.

Нужно: подготовить список GitHub Issues и план веток.

Дай:
- Markdown со списком Issues (R1‑01 … R1‑08) с Acceptance Criteria.
- Рекомендации по веткам: feature/data-drift, feature/router-deeplinks, feature/ui-home, feature/ui-list-details, feature/settings-i18n, ci/android.
- Тексты для описания PR (чек‑лист DoD).

Сначала проанализируй и скажи, есть ли вся необходимая информация.
Подготовь пошаговый план выполнения задания. Расскажи, что будешь делать. Сначала не давай примеры кода. Давай обсудим все решения, которые касаются этого задания. Только после согласования я скажу, когда надо выполнять.

Мне будут нужны готовые исправленные файлы, чтобы я просто полностью заменил текст в нужных файлах, а не искал по фрагментам.

```

---

### Небольшие советы по использованию шаблонов
- В начало каждого нового диалога **всегда включай «Context» и ссылку на репозиторий** — это гарантированно подхватит нужное ТЗ.
- Если в процессе захочешь **сузить задачу** (например, «сделай только DAO без тестов») — просто добавь строку «Из этого этапа сделать только …».
